FROM node:25.6.0-slim

# ffmpeg brings both `ffmpeg` and `ffprobe` — only the worker image carries them (per phase-03-videos/TD-03)
RUN apt update && apt install -y procps curl ffmpeg

WORKDIR /home/node/app

USER node

CMD [ "tail", "-f", "/dev/null" ]
